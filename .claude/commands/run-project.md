Run the Pomodoro Timer JavaFX application using the Maven wrapper.

Execute the following command:

```
.\mvnw javafx:run
```

Report the output to the user. If the build fails, show the relevant error messages.
